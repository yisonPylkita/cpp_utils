dist: trusty
sudo: required
branches:
  only: 
    - master
language: cpp
compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install cmake

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
- make test

